{
  "name": "delhi-biryani-automation",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "test": "echo test pass",
    "build": "mkdir -p dist && cp index.js dist/bundle.js && echo ' Biryani packed for delivery!'",
    "lint": "node -c index.js && echo ' Kitchen is clean (Syntax Checked)!'",
    "deploy": "echo ' Dispatching delivery driver...' && exit 0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}